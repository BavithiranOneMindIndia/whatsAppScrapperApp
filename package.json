{
  "name": "whatsapp-exporter",
  "version": "1.0.0",
  "description": "WhatsApp Multi-Session Exporter (Electron + Node + whatsapp-web.js)",
  "main": "main.js",
  "type": "commonjs",
  "scripts": {
    "dev": "electron .",
    "pack:win": "electron-packager . whatsapp-exporter --platform=win32 --arch=x64 --out=dist --overwrite --prune=true --icon=assets/icon.ico"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "electron-store": "^9.0.0",
    "express": "^4.19.2",
    "qrcode": "^1.5.3",
    "whatsapp-web.js": "^1.26.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  }
}
